namespace = LOGGER_CHARTS

# Enable logging
LOGGER_CHARTS.01 = {
	type = country_event
	title = LOGGER_CHARTS.01.title
	hidden = yes
	orphan = yes
	immediate = {
		set_global_variable = is_logging_enabled
		error_log = "Logging is enabled"
	}
}

# Continue logging
LOGGER_CHARTS.02 = {
    type = country_event
	title = LOGGER_CHARTS.02.title
    hidden = yes
    immediate = {
        run_logging = yes
    }
}

# Disable logging
LOGGER_CHARTS.04 = {
	type = country_event
	title = LOGGER_CHARTS.04.title
	hidden = yes
	orphan = yes
	immediate = {
		remove_global_variable = is_logging_enabled
		error_log = "Logging is disabled"
	}
}

# Enable logging local economy information
LOGGER_CHARTS.09 = {
	type = country_event
	title = LOGGER_CHARTS.01.title
	hidden = yes
	orphan = yes
	immediate = {
		set_global_variable = is_logging_local_economy_information
		error_log = "Enabled: Logging local economy information"
	}
}

# Disable logging local economy information
LOGGER_CHARTS.10 = {
	type = country_event
	title = LOGGER_CHARTS.01.title
	hidden = yes
	orphan = yes
	immediate = {
		remove_global_variable = is_logging_local_economy_information
		error_log = "Disabled: Logging local economy information"
	}
}
