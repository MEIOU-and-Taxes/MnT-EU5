namespace = LOGGER_CHARTS

# wealth, buildings, productions, pops on a market basis

# Force writing another chunk
LOGGER_CHARTS.01 = {
	title = LOGGER_CHARTS.01.title
    hidden = yes
	orphan = yes
    immediate = {
        debug_log = ""
    }
}

# Continue logging
LOGGER_CHARTS.02 = {
    type = country_event
	title = LOGGER_CHARTS.02.title
    hidden = yes
	orphan = yes
    immediate = {
		change_global_variable = { name = month add = 1 }
        run_logging = yes
    }
}

# Prepare logging
LOGGER_CHARTS.03 = {
    type = country_event
	title = LOGGER_CHARTS.03.title
    hidden = yes
	orphan = yes
    immediate = {
        set_global_variable = { name = month value = 4 }
        run_logging = yes
    }
}

# Disable logging
LOGGER_CHARTS.04 = {
	type = country_event
	title = LOGGER_CHARTS.04.title
	hidden = yes
	orphan = yes
	immediate = {
		set_global_variable = is_logging_enabled
	}
}

# Enable logging
LOGGER_CHARTS.05 = {
	type = country_event
	title = LOGGER_CHARTS.05.title
	hidden = yes
	orphan = yes
	immediate = {
		remove_global_variable = is_logging_enabled
	}
}
