namespace = mnt_credit_ai_borrowing

# Yearly AI-only cheap-credit borrowing. Uses normal estate loans.
mnt_credit_ai_borrowing.1 = {
	type = country_event
	title = hidden_event.t
	hidden = yes
	trigger = {
		is_ai = yes
		estate_loan_interest < 0.02
		gold < country_estate_loan_size
		num_loans < 6
		country_loan_capacity >= {
			value = country_estate_loan_size
			multiply = 5
		}
	}
	immediate = {
		random = {
			chance = 50
			create_estate_loan = { }
			create_estate_loan = { }
			create_estate_loan = { }
			create_estate_loan = { }
			create_estate_loan = { }
		}
	}
}
