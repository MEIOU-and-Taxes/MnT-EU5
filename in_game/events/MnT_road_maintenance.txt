namespace = road_maintenance

road_maintenance.01 = {
	type = country_event
	title = road_maintenance.01.title
	hidden = yes
	immediate = {
		calculate_road_maintenance = yes
	}
}

road_maintenance.02 = {
	type = country_event
	title = road_maintenance.02.title
	hidden = yes
	immediate = {
		if = { limit = { has_variable = road_maintenance_total }
			"estate(estate_type:nobles_estate)" = { if = { limit = { power > 0 owner = {has_variable = nobles_road_maint } }
				estate_add_gold = { value = { add = owner.var:nobles_road_maint multiply = -1 } } } }
			"estate(estate_type:clergy_estate)" = { if = { limit = { power > 0 owner = { has_variable = clergy_road_maint } }
				estate_add_gold = { value = { add = owner.var:clergy_road_maint multiply = -1 } } } }
			"estate(estate_type:burghers_estate)" = { if = { limit = { power > 0 owner = { has_variable = burghers_road_maint } }
				estate_add_gold = { value = { add = owner.var:burghers_road_maint multiply = -1 } } } }
			"estate(estate_type:peasants_estate)" = { if = { limit = { power > 0 owner = { has_variable = peasants_road_maint } }
				estate_add_gold = { value = { add = owner.var:peasants_road_maint multiply = -1 } } } }
			"estate(estate_type:tribes_estate)" = { if = { limit = { power > 0 owner = { has_variable = tribes_road_maint } }
				estate_add_gold = { value = { add = owner.var:tribes_road_maint multiply = -1 } } } }
			"estate(estate_type:dhimmi_estate)" = { if = { limit = { power > 0 owner = { has_variable = dhimmi_road_maint } }
				estate_add_gold = { value = { add = owner.var:dhimmi_road_maint multiply = -1 } } } }
			"estate(estate_type:cossacks_estate)" = { if = { limit = { power > 0 owner = { has_variable = cossacks_road_maint } }
				estate_add_gold = { value = { add = owner.var:cossacks_road_maint multiply = -1 } } } }
			add_gold = { value = var:crown_road_maint multiply = -1 }
		}
	}
}