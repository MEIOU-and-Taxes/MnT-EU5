namespace = exploration_monthly

exploration_monthly.10 = {
	type = exploration_event
	title = exploration_monthly.10.title
	desc = exploration_monthly.10.desc
	outcome = neutral

	trigger = {
		location = {
			has_owner = no
			is_coastal = yes
			is_land = yes
			OR = { climate = rainforest_climate climate = monsoon_climate climate = savanna_winter_climate climate = savanna_summer_climate }
			continent = continent:africa
		}
	}

	immediate = {
	}


	option = {
		name = exploration_monthly.10.a
		change_exploration_progress = {
			value = exploration_monthly_progress
			multiply = -2
		}

	}
}
