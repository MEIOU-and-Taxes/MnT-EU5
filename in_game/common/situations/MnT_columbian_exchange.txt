REPLACE:columbian_exchange = {
	monthly_spawn_chance = monthly_spawn_chance_unique
	hint_tag = hint_columbian_exchange
	
	can_start = {
		#At least absolutism
		current_age = age_5_absolutism
				
		#Any location in america has an owner of the old world
		continent:america = {
			any_location_in_continent = {
				population > 0
				owner_from_old_world = yes
			}
		}		
	}
	
	can_end = {
		columbian_exchange_end_trigger = yes
	}
	
	visible = {
		#Is the country from the New World?
		trigger_if = {
			limit = {
				culture = { is_culture_native_american = yes }
			}
			any_market_present_in_country = {
				save_temporary_scope_as = temp_market
				any_in_global_list = {
					variable = old_world_goods
					save_temporary_scope_as = temp_goods
					scope:temp_market = {
						OR = {
							is_produced_in_market = scope:temp_goods
							is_traded_in_market = scope:temp_goods
						}
					}
				}
			}
		}
		#or the Old World?
		trigger_else = {
			any_market_present_in_country = {
				save_temporary_scope_as = temp_market
				any_in_global_list = {
					variable = new_world_goods
					save_temporary_scope_as = temp_goods
					scope:temp_market = {
						OR = {
							is_produced_in_market = scope:temp_goods
							is_traded_in_market = scope:temp_goods
						}
					}
				}
			}
		}
	}
	

	on_start = {
		every_goods = {
			limit = { has_tag = old_world_goods }
			add_to_global_variable_list = { name = old_world_goods target = this } 
		}
		every_goods = {
			limit = { has_tag = new_world_goods }
			add_to_global_variable_list = { name = new_world_goods target = this } 
		}
		every_country = {	
			limit = {				
				NOT = { has_variable = is_in_columbian_exchange }
				any_owned_location = {
					continent = continent:america
					population > 0
				}
			}		
			#Is the country from the New World?
			if = {
				limit = {
					culture = { is_culture_native_american = yes }
					any_market_present_in_country = {
						save_temporary_scope_as = temp_market
						any_in_global_list = {
							variable = old_world_goods
							save_temporary_scope_as = temp_goods
							scope:temp_market = {
								OR = {
									is_produced_in_market = scope:temp_goods
									is_traded_in_market = scope:temp_goods
								}
							}
						}
					}
				}
				set_variable = { name = is_in_columbian_exchange value = yes }
				trigger_event_non_silently = columbian_exchange.1
			}
			#or the Old World?
			else_if = {
				limit = {
					NOT = { culture = { is_culture_native_american = yes } }
					any_market_present_in_country = {
						save_temporary_scope_as = temp_market
						any_in_global_list = {
							variable = new_world_goods
							save_temporary_scope_as = temp_goods
							scope:temp_market = {
								OR = {
									is_produced_in_market = scope:temp_goods
									is_traded_in_market = scope:temp_goods
								}
							}
						}
					}

				}
				set_variable = { name = is_in_columbian_exchange value = yes }
				trigger_event_non_silently = columbian_exchange.1
			}	
		}	
	}

	on_monthly = {
		every_country = {	
			limit = {				
				NOT = { has_variable = is_in_columbian_exchange}
				any_owned_location = {
					continent = continent:america
					population > 0
				}
			}		
			#Is the country from the New World?
			if = {
				limit = {
					culture = { is_culture_native_american = yes }
					any_market_present_in_country = {
						save_temporary_scope_as = temp_market
						any_in_global_list = {
							variable = old_world_goods
							save_temporary_scope_as = temp_goods
							scope:temp_market = {
								OR = {
									is_produced_in_market = scope:temp_goods
									is_traded_in_market = scope:temp_goods
								}
							}
						}
					}
				}
				set_variable = { name = is_in_columbian_exchange value = yes }
				trigger_event_non_silently = columbian_exchange.1
			}
			#or the Old World?
			else_if = {
				limit = {
					NOT = {culture = { is_culture_native_american = yes }}
					any_market_present_in_country = {
						save_temporary_scope_as = temp_market
						any_in_global_list = {
							variable = new_world_goods
							save_temporary_scope_as = temp_goods
							scope:temp_market = {
								OR = {
									is_produced_in_market = scope:temp_goods
									is_traded_in_market = scope:temp_goods
								}
							}
						}
					}

				}
				set_variable = { name = is_in_columbian_exchange value = yes }
				trigger_event_non_silently = columbian_exchange.1
			}	
		}	
	}

	tooltip = {
	}


	on_ended = {
		every_country = {	
			limit = {				
				has_variable = is_in_columbian_exchange
			}	
			remove_variable = is_in_columbian_exchange
		}	
		every_location = {
			limit = {
				OR = {
					has_variable = changed_to_old_world_rgo
					has_variable = changed_to_new_world_rgo
				}
			}
			remove_variable = changed_to_old_world_rgo
			remove_variable = changed_to_new_world_rgo
			remove_variable = columbian_exchange_old_raw_material
		}
	}
	
	map_color = {
		if = {
			limit = {
				has_variable = changed_to_new_world_rgo
			}
			if = {
				limit = {
					raw_material = goods:tobacco
				}
				value = goods_tobacco
			}
			else_if = {
				limit = {
					raw_material = goods:cocoa
				}
				value = goods_cocoa
			}
			else_if = {
				limit = {
					raw_material = goods:maize
				}
				value = goods_maize
			}
			else_if = {
				limit = {
					raw_material = goods:chili
				}
				value = goods_chili
			}
			else_if = {
				limit = {
					raw_material = goods:potato
				}
				value = goods_potato
			}
			else = {
				value = color_nw_country
			}
		}
		else_if = {
			limit = {
				has_variable = changed_to_old_world_rgo
			}
			if = {
				limit = {
					raw_material = goods:cotton
				}
				value = goods_cotton
			}
			else_if = {
				limit = {
					raw_material = goods:wheat
				}
				value = goods_wheat
			}
			else_if = {
				limit = {
					raw_material = goods:millet
				}
				value = goods_millet
			}
			else_if = {
				limit = {
					raw_material = goods:olives
				}
				value = goods_olives
			}
			else_if = {
				limit = {
					raw_material = goods:coffee
				}
				value = goods_coffee
			}
			else_if = {
				limit = {
					raw_material = goods:rice
				}
				value = goods_rice
			}
			else_if = {
				limit = {
					raw_material = goods:sugar
				}
				value = goods_sugar
			}
			else_if = {
				limit = {
					raw_material = goods:wine
				}
				value = goods_wine
			}
			else_if = {
				limit = {
					raw_material = goods:horses
				}
				value = goods_horses
			}
			else_if = {
				limit = {
					raw_material = goods:livestock
				}
				value = goods_livestock
			}
			else_if = {
				limit = {
					raw_material = goods:saffron
				}
				value = goods_saffron
			}
			else_if = {
				limit = {
					raw_material = goods:pepper
				}
				value = goods_pepper
			}
			else_if = {
				limit = {
					raw_material = goods:cloves
				}
				value = goods_cloves
			}
			else = {
				value = color_ow_country
			}
		}
		#Neutral
		else = {
			value = define:NMapColors|DEFAULT_COLOR
		}
	}

	tooltip = {
		if = {
			limit = {	
				has_owner = yes
				OR = {
					has_variable = changed_to_new_world_rgo
					has_variable = changed_to_old_world_rgo
				}
			}
			custom_tooltip = "columbian_exchange_replaced_rgo"
		}
	}

	legend_key = { 
		desc = "tobacco"
		color = goods_tobacco
	}
	legend_key = { 
		desc = "cocoa"
		color = goods_cocoa
	}
	legend_key = { 
		desc = "maize"
		color = goods_maize
	}
	legend_key = { 
		desc = "chili"
		color = goods_chili
	}
	legend_key = { 
		desc = "potato"
		color = goods_potato
	}
	legend_key = { 
		desc = "cotton"
		color = goods_cotton
	}
	legend_key = { 
		desc = "wheat"
		color = goods_wheat
	}
	legend_key = { 
		desc = "millet"
		color = goods_millet
	}
	legend_key = { 
		desc = "olives"
		color = goods_olives
	}
	legend_key = { 
		desc = "coffee"
		color = goods_coffee
	}
	legend_key = { 
		desc = "rice"
		color = goods_rice
	}
	legend_key = { 
		desc = "sugar"
		color = goods_sugar
	}
	legend_key = { 
		desc = "wine"
		color = goods_wine
	}
	legend_key = { 
		desc = "horses"
		color = goods_horses
	}
	legend_key = { 
		desc = "livestock"
		color = goods_livestock
	}
	legend_key = { 
		desc = "saffron"
		color = goods_saffron
	}
	legend_key = { 
		desc = "pepper"
		color = goods_pepper
	}
	legend_key = { 
		desc = "cloves"
		color = goods_cloves
	}
}
