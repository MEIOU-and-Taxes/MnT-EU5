yearly_country_pulse = {
	trigger = { c:FRA ?= this }
	on_actions = {
		once_per_year
	}
}

once_per_year = {
	trigger = { c:FRA ?= this }
	effect = {
		c:FRA = {
			trigger_event_silently = { id = LOGGER_CHARTS.02 }
		}
	}
}
