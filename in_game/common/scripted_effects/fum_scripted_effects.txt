fum_remove_sop_effect = {
    every_country = {
        limit = {
            country_type = pop
        }
        change_country_type = building
        every_owned_building = {
            change_building_level = -10
        }
    }
}