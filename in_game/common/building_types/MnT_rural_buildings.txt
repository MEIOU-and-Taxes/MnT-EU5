REPLACE:bailiff = {
	is_foreign = no
	max_levels = 1
	pop_type = soldiers
	
	employment_size = bailiff_employment
	category = government_category
	
	rural_settlement = yes
	town = yes
	city = yes
	expensive = yes
	build_time = rural_build_time
	
	forbidden_for_estates = yes
	
	location_potential = {
		is_overseas_for_owner = no
	}
	
	
	unique_production_methods = {
		bailiff_input = {
			paper = 0.1
			tools = 0.05
			weaponry = 0.2
			stone = 0.1
			category = building_maintenance
		}
	}

	modifier = {
		local_proximity_source = 20
		local_nobles_estate_power = 1.0
	}
	
	
}

#Entirely rebuilt by M&T, old version is now common_pasture
REPLACE:farming_village = {
	is_foreign = no
	max_levels = rural_building_cap
	pop_type = peasants
	
	category = village_category
	employment_size = 5
	
	rural_settlement = yes
	
	location_potential = {
		NOT = {
			vegetation = desert
			topography = mountains
			climate = arctic
		}
	}
	
	build_time = village_build_time

	construction_demand = village_construction

	modifier = {
		local_food_capacity = 100
		local_monthly_food = 10.0	
		local_peasants_estate_power = 0.1
	}
}

#created by M&T, was farming_village
common_pasture = {
	is_foreign = no
	max_levels = rural_building_cap
	pop_type = peasants

	category = village_category
	employment_size = village_employment_size
	
	rural_settlement = yes
	
	#heavily changed location requirements
	location_potential = {
		NOT = {
			vegetation = desert
			vegetation = forest
			vegetation = jungle
			topography = wetlands
			topography = mountains
			climate = arctic
		}
	}
	
	build_time = village_build_time
	
	unique_production_methods = {
		common_pasture_maintenance = {
			tools = 0.025
			clay = 1.0
			
			output = 0.5
			
			produced = livestock
			
			debug_max_profit = 0.6
	
			category = building_maintenance
		}
	}	
	
	construction_demand = village_construction

	modifier = {
		local_peasants_estate_power = 0.1		#M&T from 0.25
		
	}
}