REPLACE:theodosian_walls = {
	is_special = yes
	is_foreign = no
	pop_type = soldiers
	max_levels = theodosian_walls_max_level
	
	employment_size = theodosian_walls
	category = defense_category
	
	
	rural_settlement = no
	town = no
	city = yes
	megalopolis = yes
	build_time = huge_unique_build_time

	location_potential = {
		this = location:constantinople
	}
	
	country_potential = {
		owns = location:constantinople
	}

	allow = {
		owner = {
			or = {
				is_ai = no
				not = { has_or_had_tag = TUR }
				and = {
					has_or_had_tag = TUR
					c:TUR.religion = location:constantinople.dominant_religion	
				}
			}
		}
	}

	remove_if = {
		root.owner = {
			is_ai = yes
			has_or_had_tag = TUR
			not = { 
				c:TUR.religion = location:constantinople.dominant_religion 
			}
		}
	}

	expensive = yes
	possible_production_methods = {
		city_walls_maintenance
	}
	construction_demand = city_walls_construction

	modifier = {
		# same as city_walls but it can be doubled by event
		local_garrison_size = 1.0
		local_defensive = 0.25
		blocks_vision_from_land = yes
		blocks_vision_from_sea = yes
		movement_cost = 0.2
		local_unrest = -0.05
		local_burghers_estate_power = 0.1
	}

	market_center_modifier = {
		local_trade_center_power = 0.05     # M&T halved from 0.1	
	}
	
	raw_modifier = {
		fort_level = 2
		pure_tooltip_entry = pte_no_propagating_zone_of_control            # propagating_zone_of_control = no
	}
	graphical_tags = { large_city_walls city_walls }
}
