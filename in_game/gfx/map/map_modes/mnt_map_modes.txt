@zoom_step_flatmap_far = 	21 	# the furthest zoom out
@zoom_step_flatmap = 		12 	# zoom step where the paper map kicks in
@zoom_step_far = 			11	# furthest zoom before the paper map
@zoom_step_mid = 			4 	# intorduce some color
@zoom_step_near = 			2	# closest zoom

# Zoomed in terrain map
@hollow_near_gradient_alpha_inside = 	0
@hollow_near_gradient_alpha_outside = 	0			
@hollow_near_gradient_width = 			0.0      	
@hollow_near_gradient_color_mult = 		0.25     
@hollow_near_edge_width = 				0      
@hollow_near_edge_sharpness = 			0.0000     
@hollow_near_edge_alpha = 				0.5
@hollow_near_edge_color_mult = 			0.7
@hollow_near_before_lighting_blend = 	0.5
@hollow_near_after_lighting_blend = 	0

# Mid zoom terrain map
@hollow_mid_gradient_alpha_inside = 	0.00
@hollow_mid_gradient_alpha_outside = 	0.25
@hollow_mid_gradient_width = 			0.1
@hollow_mid_gradient_color_mult = 		@hollow_near_gradient_color_mult
@hollow_mid_edge_width = 				0.00
@hollow_mid_edge_sharpness = 			@hollow_near_edge_sharpness
@hollow_mid_edge_alpha = 				@hollow_near_edge_alpha
@hollow_mid_edge_color_mult = 			@hollow_near_edge_color_mult
@hollow_mid_before_lighting_blend = 	@hollow_near_before_lighting_blend 
@hollow_mid_after_lighting_blend = 		@hollow_near_after_lighting_blend

# Zoomed out terrain map
@hollow_far_gradient_alpha_inside = 	0.25 
@hollow_far_gradient_alpha_outside = 	1
@hollow_far_gradient_width = 			0.25
@hollow_far_gradient_color_mult = 		@hollow_mid_gradient_color_mult
@hollow_far_edge_width = 				0.0
@hollow_far_edge_sharpness = 			@hollow_mid_edge_sharpness
@hollow_far_edge_alpha = 				0.0
@hollow_far_edge_color_mult = 			@hollow_mid_edge_color_mult
@hollow_far_before_lighting_blend = 	@hollow_mid_before_lighting_blend
@hollow_far_after_lighting_blend = 		@hollow_mid_after_lighting_blend

# Paper map
@flatmap_gradient_alpha_inside = 		0.75
@flatmap_gradient_alpha_outside = 		1
@flatmap_gradient_width = 				0.25
@flatmap_gradient_color_mult = 			0.9 
@flatmap_edge_width = 					0.03
@flatmap_edge_sharpness = 				0.01
@flatmap_edge_alpha = 					1.0
@flatmap_edge_color_mult = 				@hollow_near_edge_color_mult
@flatmap_before_lighting_blend = 		0.5
@flatmap_after_lighting_blend = 		0.5 

# Paper map (far out)
@flatmap_far_gradient_alpha_inside = 	@flatmap_gradient_alpha_inside
@flatmap_far_gradient_alpha_outside = 	@flatmap_gradient_alpha_outside
@flatmap_far_gradient_width = 			@[flatmap_edge_color_mult*1.25]
@flatmap_far_gradient_color_mult = 		@flatmap_gradient_color_mult
@flatmap_far_edge_width = 				@[flatmap_edge_width*1.5]
@flatmap_far_edge_sharpness = 			@flatmap_edge_sharpness
@flatmap_far_edge_alpha = 				@flatmap_edge_alpha
@flatmap_far_edge_color_mult = 			@flatmap_edge_color_mult
@flatmap_far_before_lighting_blend = 	@flatmap_before_lighting_blend
@flatmap_far_after_lighting_blend = 	@flatmap_after_lighting_blend 

# Zoomed out political map
@filled_far_gradient_alpha_inside = 	1
@filled_far_gradient_alpha_outside = 	1
@filled_far_gradient_width = 			1
@filled_far_gradient_color_mult = 		0.4
@filled_far_edge_width = 				0.03
@filled_far_edge_sharpness = 			@hollow_mid_edge_sharpness
@filled_far_edge_alpha = 				1
@filled_far_edge_color_mult = 			0.3
@filled_far_before_lighting_blend = 	1
@filled_far_after_lighting_blend = 		0.5
# Zoomed in political map
@filled_near_gradient_alpha_inside = 	1
@filled_near_gradient_alpha_outside = 	1
@filled_near_gradient_width = 			1
@filled_near_gradient_color_mult = 		0.4
@filled_near_edge_width = 				0.03
@filled_near_edge_sharpness = 			@hollow_mid_edge_sharpness
@filled_near_edge_alpha = 				1
@filled_near_edge_color_mult = 			0.3
@filled_near_before_lighting_blend = 	1
@filled_near_after_lighting_blend = 	0.25

# Map mode that works well with data and heatmaps
@data_gradient_alpha_inside = 			1
@data_gradient_alpha_outside = 			1
@data_gradient_width = 					@flatmap_far_gradient_width
@data_gradient_color_mult = 			@flatmap_far_gradient_color_mult
@data_edge_width = 						0
@data_edge_sharpness = 					@flatmap_far_edge_sharpness
@data_edge_alpha = 						0
@data_edge_color_mult = 				0
@data_before_lighting_blend = 			@flatmap_far_before_lighting_blend
@data_after_lighting_blend = 			@flatmap_far_after_lighting_blend

max_tax_base = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 			is_owned_or_owned_by_subjects_or_below_of = scope:actor
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = location_tax_base
					divide = local_control
					subtract = 0
					divide = 70 #every location with potential tax base >= 70 will be fully green, it can be adjusted to get better fidelity
				}
			}
		}
	}

	tooltip_key = {

	}

	small_map_names = location
	medium_map_names = location
	large_map_names = location

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location
	enable_rivers = yes

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 0

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no 
		unit_marker = no
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	color_and_names_refresh_counters = {}
}

max_tax_base_global = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = location_tax_base
					divide = local_control
					subtract = 0
					divide = 70 #every location with potential tax base >= 70 will be fully green, it can be adjusted to get better fidelity
				}
			}
		}
	}

	tooltip_key = {

	}
	
	small_map_names = location
	medium_map_names = location
	large_map_names = location

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location
	enable_rivers = yes

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 0

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
		unit_marker = no
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	color_and_names_refresh_counters = {}
}

REPLACE:rivers = {
	color_mode = rivers
	small_map_names = none
	medium_map_names = none
	large_map_names = none

	small_tooltip_context = none
	medium_tooltip_context = none
	large_tooltip_context = none

	fill_in_impassable = yes
	flatmap_behaviour = Always
	enable_rivers = yes
	use_fow = no
	
	category = geography

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
}

market_access = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = market_access
					subtract = 0.5
					multiply = 2
				}
			}
		}
	}

	tooltip_key = {

	}
	
	small_map_names = location
	medium_map_names = market
	large_map_names = market

	small_tooltip_context = location
	medium_tooltip_context = market
	large_tooltip_context = market
	enable_rivers = yes

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 3

	map_markers = {
		all = no
		market_marker = yes
		toll_marker = yes
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { Month }
	color_and_names_refresh_counters = { MarketReach LocationOwnerChanged }
	map_lines_mode = ToMarketCenter
}

REPLACE:stability = {
	map_color = {
		if = {
			limit = {
				has_owner = yes
			}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH
				factor = {
					value = owner.stability
					add = 25 # everything below -25 is red
					divide = 75 # everything above +50 is green
				}
			}
		}
	}
	tooltip_key = {
	}
	
	small_map_names = location
	medium_map_names = country
	large_map_names = country

	small_tooltip_context = country
	medium_tooltip_context = country
	large_tooltip_context = country

	flatmap_behaviour = Always
	use_fow = no

	category = government
	index = 3

	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
		flatmap_gradient_parameters = {
		zoom_step = @zoom_step_flatmap

		gradient_alpha_inside = 	@flatmap_gradient_alpha_inside
		gradient_alpha_outside = 	@flatmap_gradient_alpha_outside
		gradient_width = 			@flatmap_gradient_width
		gradient_color_mult =		@flatmap_gradient_color_mult
		edge_width = 				@flatmap_edge_width
		edge_sharpness = 			@flatmap_edge_sharpness
		edge_alpha = 				@flatmap_edge_alpha
		edge_color_mult = 			@flatmap_edge_color_mult
		before_lighting_blend =		@flatmap_before_lighting_blend
		after_lighting_blend =		@flatmap_after_lighting_blend
	}
	flatmap_gradient_parameters = {
		zoom_step = @zoom_step_flatmap_far

		gradient_alpha_inside = 	@flatmap_far_gradient_alpha_inside
		gradient_alpha_outside = 	@flatmap_far_gradient_alpha_outside
		gradient_width = 			@flatmap_far_gradient_width 
		gradient_color_mult =		@flatmap_far_gradient_color_mult 
		edge_width = 				@flatmap_far_edge_width 		
		edge_sharpness = 			@flatmap_far_edge_sharpness 	
		edge_alpha = 				@flatmap_far_edge_alpha 	
		edge_color_mult = 			@flatmap_far_edge_color_mult 
		before_lighting_blend =		@flatmap_far_before_lighting_blend 
		after_lighting_blend =		@flatmap_far_after_lighting_blend 
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { Day }
}

missing_control = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 			is_owned_or_owned_by_subjects_or_below_of = scope:actor
				max_control != local_control
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = max_control
					subtract = local_control
					add = 0.25 #0 for 25% control missing, 1.25 for 100% excess
					divide = 1.25 #0 for 25% control missing, 1 for 25% excess
				}
			}
		}
	}

	legend_key = {
		desc = "CURRENT_CONTROL_BELOW_MAX"
		color = define:NMapColors|MAP_COLOR_HIGH
	}
	legend_key = {
		desc = "CURRENT_CONTROL_ABOVE_MAX"
		color = define:NMapColors|MAP_COLOR_LOW
	}

	small_map_names = location
	medium_map_names = province
	large_map_names = province

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location
	enable_rivers = yes
	
	map_lines_mode = ToCapital #experimental

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 2

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no 
		unit_marker = no
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { LocationControlChanged }
	color_and_names_refresh_counters = { LocationOwnerChanged CountryStatus }
}

max_control = {
	map_color = {
		if = {
			limit = {
				has_owner = yes
				owner = scope:actor
			}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = max_control
				}
			}
		}
	}
	tooltip_key = {
	}

	small_map_names = location_control
	medium_map_names = location_control
	large_map_names = province_control

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location
	enable_rivers = yes

	map_lines_mode = ToCapital #experimental

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 2

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
		unit_marker = no
	}

	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}

	refresh_colors_on_selection_change = yes
	color_refresh_counters = { LocationControlChanged }
	color_and_names_refresh_counters = { LocationOwnerChanged CountryStatus }
}

max_control_with_location_rank = {
	map_color = {
		if = {
			limit = {
				has_owner = yes
				owner = scope:actor
			}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = max_control
				}
			}
		}
	}
	secondary_map_color = {
		if = {
			limit = { 
				has_owner = yes
				owner = scope:actor
				location_rank = location_rank:rural_settlement 
			}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = max_control
				}
			}
		}
		else_if = {
			limit = { 				
				has_owner = yes
				owner = scope:actor
				location_rank = location_rank:town 
			}
			value = blue #grey in Location Rank vanilla mapmode, but it is overlapping with colors present in lerp
		}
		else_if = {
			limit = { 				
				has_owner = yes
				owner = scope:actor
				location_rank = location_rank:city 
			}
			value = purple #yellow in Location Rank vanilla mapmode, but it is overlapping with colors present in lerp
		}
	}

	tooltip_key = {
	}

	legend_key = {
		desc = "town"
		color = purple
	}

	legend_key = {
		desc = "city"
		color = blue
	}

	small_map_names = location_control
	medium_map_names = location_control
	large_map_names = province_control

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location
	enable_rivers = yes

	map_lines_mode = ToCapital #experimental

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 2

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
		unit_marker = no
	}

	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}

	refresh_colors_on_selection_change = yes
	color_refresh_counters = { LocationControlChanged }
	color_and_names_refresh_counters = { LocationOwnerChanged CountryStatus }
}

nobles_percentage = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 			is_owned_or_owned_by_subjects_or_below_of = scope:actor
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = num_pop_type:nobles
					divide = population
					multiply = 50 #saturation at 2%
				}
			}
		}
	}

	legend_key = {
		desc = "POPULATION_SHARE_ABOVE_2PERC"
		color = define:NMapColors|MAP_COLOR_HIGH
	}
	legend_key = {
		desc = "POPULATION_SHARE_AT_0PERC"
		color = define:NMapColors|MAP_COLOR_LOW
	}

	pop_type = nobles
	small_map_names = location_pop_type
	medium_map_names = province_pop_type
	large_map_names = country_pop_type

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location

	flatmap_behaviour = Always
	use_fow = no

	category = population
	index = 1

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
		unit_marker = no
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { Day }
	color_and_names_refresh_counters = { LocationPopulationChanged }
	
	#map_lines_mode = Migration
}

burghers_percentage = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 			is_owned_or_owned_by_subjects_or_below_of = scope:actor
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = num_pop_type:burghers
					divide = population
					multiply = 2 #saturation at 50%
				}
			}
		}
	}

	legend_key = {
		desc = "POPULATION_SHARE_ABOVE_50PERC"
		color = define:NMapColors|MAP_COLOR_HIGH
	}
	legend_key = {
		desc = "POPULATION_SHARE_AT_0PERC"
		color = define:NMapColors|MAP_COLOR_LOW
	}

	pop_type = burghers
	small_map_names = location_pop_type
	medium_map_names = province_pop_type
	large_map_names = country_pop_type

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location

	flatmap_behaviour = Always
	use_fow = no

	category = population
	index = 1

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
		unit_marker = no
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { Day }
	color_and_names_refresh_counters = { LocationPopulationChanged }
	
	#map_lines_mode = Migration
}

laborers_percentage = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 			is_owned_or_owned_by_subjects_or_below_of = scope:actor
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = num_pop_type:laborers
					divide = population #no further division, saturation at 100%
				}
			}
		}
	}

	legend_key = {
		desc = "POPULATION_SHARE_AT_100PERC"
		color = define:NMapColors|MAP_COLOR_HIGH
	}
	legend_key = {
		desc = "POPULATION_SHARE_AT_0PERC"
		color = define:NMapColors|MAP_COLOR_LOW
	}
	
	pop_type = laborers
	small_map_names = location_pop_type
	medium_map_names = province_pop_type
	large_map_names = country_pop_type

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location

	flatmap_behaviour = Always
	use_fow = no

	category = population
	index = 1

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
		unit_marker = no
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { Day }
	color_and_names_refresh_counters = { LocationPopulationChanged }
	
	map_lines_mode = Migration
}

peasants_percentage = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 			is_owned_or_owned_by_subjects_or_below_of = scope:actor
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = num_pop_type:peasants
					divide = population #no further division, saturation at 100%
				}
			}
		}
	}

	legend_key = {
		desc = "POPULATION_SHARE_AT_100PERC"
		color = define:NMapColors|MAP_COLOR_HIGH
	}
	legend_key = {
		desc = "POPULATION_SHARE_AT_0PERC"
		color = define:NMapColors|MAP_COLOR_LOW
	}
	
	pop_type = peasants
	small_map_names = location_pop_type
	medium_map_names = province_pop_type
	large_map_names = country_pop_type

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location

	flatmap_behaviour = Always
	use_fow = no

	category = population
	index = 1

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
		unit_marker = no
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { Day }
	color_and_names_refresh_counters = { LocationPopulationChanged }
	
	map_lines_mode = Migration
}

market_food_balance = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = market.market_monthly_food_balance
					divide = market.market_population
				}
			}
		}
	}

	legend_key = {
		desc = "FOOD_BALANCE_ABOVE_1UNIT_PER_POP"
		color = define:NMapColors|MAP_COLOR_HIGH
	}
	legend_key = {
		desc = "FOOD_BALANCE_NEGATIVE"
		color = define:NMapColors|MAP_COLOR_LOW
	}

	small_map_names = location
	medium_map_names = market
	large_map_names = market

	small_tooltip_context = location
	medium_tooltip_context = market
	large_tooltip_context = market
	enable_rivers = yes

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 3

	map_markers = {
		all = no
		market_marker = yes
		toll_marker = yes
		raw_goods_marker = yes
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { Month }
	color_and_names_refresh_counters = { MarketReach LocationOwnerChanged }
	map_lines_mode = ToMarketCenter
}

market_food_stockpile = {
	map_color = {	
	 	if = {
	 		limit = {
	 			has_owner = yes
	 		}
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH

				factor = {
					value = market.market_food
					divide = market.market_population
					divide = 20
				}
			}
		}
	}

	legend_key = {
		desc = "FOOD_STOCKPILE_ABOVE_20_UNIT_PER_POP"
		color = define:NMapColors|MAP_COLOR_HIGH
	}
	legend_key = {
		desc = "FOOD_STOCKPILE_AT_0"
		color = define:NMapColors|MAP_COLOR_LOW
	}
	
	small_map_names = location
	medium_map_names = market
	large_map_names = market

	small_tooltip_context = location
	medium_tooltip_context = market
	large_tooltip_context = market
	enable_rivers = yes

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 3

	map_markers = {
		all = no
		market_marker = yes
		toll_marker = yes
	}
	
	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}
	
	refresh_colors_on_selection_change = yes
	color_refresh_counters = { Month }
	color_and_names_refresh_counters = { MarketReach LocationOwnerChanged }
	map_lines_mode = ToMarketCenter
}

road_maintenance_cost = {
	map_color = {
	 	if = {
	 		limit = {
	 			is_land = yes
				has_variable = road_maintenance_cost
	 		}
			if = {
				limit = { mnt_road_maintenance_cost_relative_to_max > 0.66 }
				lerp = {
					min_color = define:NMapColors|MAP_COLOR_LOW
					max_color = define:NMapColors|MAP_COLOR_MIN

					factor = {
						value = mnt_road_maintenance_cost_relative_to_max
						subtract = 0.66
						multiply = 3
					}
				}
			}
			else_if = {
				limit = { mnt_road_maintenance_cost_relative_to_max > 0.33 }
				lerp = {
					min_color = define:NMapColors|MAP_COLOR_MID
					max_color = define:NMapColors|MAP_COLOR_LOW

					factor = {
						value = mnt_road_maintenance_cost_relative_to_max
						subtract = 0.33
						multiply = 3
					}
				}
			}
			else = {
				lerp = {
					min_color = define:NMapColors|MAP_COLOR_HIGH
					max_color = define:NMapColors|MAP_COLOR_MID

					factor = {
						value = mnt_road_maintenance_cost_relative_to_max
						multiply = 3
					}
				}
			}
		}
	}

	tooltip_key = {

	}

	small_map_names = location
	medium_map_names = location
	large_map_names = location

	small_tooltip_context = location
	medium_tooltip_context = location
	large_tooltip_context = location
	enable_rivers = yes

	flatmap_behaviour = Always
	use_fow = no

	category = economy
	index = 0

	legend_key = {
		desc = "ROAD_MAINTENANCE_BLACK"
		color = define:NMapColors|MAP_COLOR_MIN
	}
	legend_key = {
		desc = "ROAD_MAINTENANCE_RED"
		color = define:NMapColors|MAP_COLOR_LOW
	}
	legend_key = {
		desc = "ROAD_MAINTENANCE_YELLOW"
		color = define:NMapColors|MAP_COLOR_MID
	}
	legend_key = {
		desc = "ROAD_MAINTENANCE_GREEN_DARK"
		color = define:NMapColors|MAP_COLOR_HIGH
	}

	map_markers = {
		market_marker = no
		fort_marker = no
		toll_marker = no
		unit_marker = no
	}

	gradient_parameters = {
		zoom_step = @zoom_step_near

		gradient_alpha_inside = 	@data_gradient_alpha_inside
		gradient_alpha_outside = 	@data_gradient_alpha_outside
		gradient_width = 			@data_gradient_width
		gradient_color_mult =		@data_gradient_color_mult
		edge_width = 				@data_edge_width
		edge_sharpness = 			@data_edge_sharpness
		edge_alpha = 				@data_edge_alpha
		edge_color_mult = 			@data_edge_color_mult
		before_lighting_blend =		@data_before_lighting_blend
		after_lighting_blend =		@data_after_lighting_blend
	}

	color_and_names_refresh_counters = {}
}