namespace = TEST_MONTHLY

# wealth, buildings, productions, pops on a market basis

TEST_MONTHLY.FORCE_LOGGING = {
    hidden = yes
    immediate = {
        debug_log = ""
    }
}

TEST_MONTHLY.02 = {
    type = country_event
    hidden = yes

    immediate = {
        change_global_variable = { name = year add = 1 }
        run_logging = yes
    }
}

TEST_MONTHLY.03 = {
    hidden = yes
    immediate = {
        clear_global_variable_list = strategic_regs
        set_global_variable = { name = year value = 0 }
        prepare_strategic_regions = yes
        count_states_in_strategic_regions = yes
        run_logging = yes
    }
}
